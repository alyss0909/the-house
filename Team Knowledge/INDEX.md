# Team Knowledge - Master Hub

This is the operations side of your myPKA. It holds the team's procedures, orchestrations, reference material, and session history. The user's personal knowledge lives in [[PKM/INDEX]].

## Sections

- **[[Team Knowledge/SOPs/INDEX|SOPs]]** — agent skills. Canonical step-by-step procedures, one job per file, LLM-agnostic and reusable across agents. Each SOP has a default owner, but any agent can invoke it. Think of SOPs the way Claude skills work. Filenames: `SOP-NNN-<title>.md`.
- **[[Team Knowledge/Workstreams/INDEX|Workstreams]]** — multi-agent compositions. Recurring orchestrations where more than one specialist collaborates. Workstreams string SOPs together — think of them the way Claude plugins compose skills. Emergent: the scaffold ships only canonical day-1 flows; new Workstreams get authored when a pattern repeats. Filenames: `WS-NNN-<title>.md`.
- **[[Team Knowledge/Guidelines/INDEX|Guidelines]]** — general rules every agent reads. Static constraints (naming, frontmatter, design system) that SOPs and Workstreams `[[wikilink]]` to rather than duplicate. Filenames: `GL-NNN-<title>.md`.
- **[[Team Knowledge/Automations/|Automations]]** — standalone automation scripts (PowerShell, etc.) that run outside Obsidian. Current scripts: `meetily-weekly-launcher.ps1` (weekly Meetily auto-launch from Google Calendar).
- **session-logs/** — append-only record of every working session, written by Larry. Path: `session-logs/YYYY/MM/YYYY-MM-DD-<slug>.md`.
- **[[Team Knowledge/Weekly Reviews/README|Weekly Reviews]]** — weekly wrap-up documents aggregating session logs and deliverables. Generated by `/weekly-review` (WS-006). Path: `Weekly Reviews/YYYY/YYYY-WNN-weekly-wrap.md`.
- **[[Team Knowledge/Monthly Reviews/README|Monthly Reviews]]** — monthly wrap-up documents synthesizing weekly wraps. Generated by `/monthly-review` (WS-007). Reads weekly wraps only — never raw session logs. Path: `Monthly Reviews/YYYY/YYYY-MM-monthly-wrap.md`.

## Scripts folders (intentionally separate)

There are two scripts locations in this scaffold. Do not merge them.

- **`scripts/`** (at vault root) — scaffold-level build and release scripts. These operate on the scaffold itself: `fix_connections.py` (wikilink repair), `validation-script.sh` (CI/CD validation). Owned by scaffold maintainers; not part of daily team operations.
- **`Team Knowledge/scripts/`** — operational team scripts. These run as part of daily team work: Telegram integrations, knowledge imports, data migrations (e.g. `import_idea_pantry.py`, `migrate-inline-fields-to-frontmatter.py`). Owned by the active team specialists (Silas, Mack).

## Taxonomy in plain English

- An **SOP** is an agent skill. It answers "how do I do X?" in clear steps. Like a Claude skill — discrete, named, callable. Default owner runs it most often; any agent can invoke it.
- A **Workstream** is a multi-agent composition. It answers "how do we deliver X together, recurring?" Like a Claude plugin — strings skills into a flow. Ships only when the pattern is canonical; new ones emerge from repeated session-log patterns.
- A **Guideline** is a general rule. It answers "what is the rule for X?" Static reference every relevant agent reads. Never a procedure.

When in doubt: write a Guideline first if the rule is static. Write an SOP if the procedure has steps and one default owner. Write a Workstream only when more than one specialist is involved AND the pattern repeats.

## SSOT applies here too

If naming rules belong in [[GL-001-file-naming-conventions]], do not restate them inside an SOP or Workstream. Link to the Guideline instead.

## Cross-session learnings

When the team learns something durable across sessions, Larry appends it to a "Cross-session learnings" section at the bottom of this file. Session-specific notes stay in the session log under `session-logs/YYYY/MM/`.

### Cross-session learnings

- If Codex Desktop on Windows suddenly cannot run new local commands and reports `windows sandbox: setup refresh failed` or `windows sandbox failed: spawn setup refresh`, first check [[SOP-012-codex-windows-sandbox-recovery]] before reinstalling Codex. The June 2026 incident was caused by a corrupted `.codex/.sandbox/deny_read_acl_state.json` plus elevated sandbox mode.
- For any dashboard/CSS/UI fix in this vault, verify with a live Obsidian CLI screenshot (`open path=` → `eval setState({mode:'preview'})` → `dev:screenshot`) before reporting it done — see [[reference_obsidian_cli_screenshots]] in cross-session memory. A specialist's code-level "fixed" report is not proof; on 2026-07-07 a duplicate-title bug on the Arc dashboard survived one full fix pass that was reported as resolved, and only a live screenshot caught it.
- Don't ask Alyssa's permission to fix an obviously-unwanted defect (a duplicate line, a broken parity rule already locked elsewhere). Reserve questions for genuine judgment calls where a reasonable person could land on either answer. She called this out directly on 2026-07-07 — see [[feedback_dont_punt_known_fixes]] in cross-session memory.

## Active session log

The current session log lives in `session-logs/YYYY/MM/`. Larry writes one per session at close.
